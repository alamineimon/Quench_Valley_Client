import React from 'react'

const ggfhfgh = () => {
  return (
    <div>
        <h1>ddyusi</h1>
{/*         
        const updateUser = {
          firstName,
          lastName,
          email,
          phoneNumber,
          bio,
          photoUrl
        };
        fetch("http://localhost:5000/users", {
          method: "PUT",
          headers: {
            "content-type": "application/json",
          },
          body: JSON.stringify(updateUser)
        })
          .then((res) => res.json())
          .then((data) => {
            console.log(data)
            // if (data.acknowledged) {
            //   toast("Successfully updated");
            //   form.reset();
            // }

      }) */}
    </div>
  )
}

export default ggfhfgh